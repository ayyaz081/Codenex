# ===========================================
# SAME DOMAIN HOSTING - FRONTEND + BACKEND TOGETHER
# ===========================================
# Use this when hosting both frontend and backend on the same domain
# Example: yoursite.com (frontend) + yoursite.com/api (backend)

# 🔐 SECURITY (Required)
JWT_KEY=gD73HWcT/5WUpQGXin4O7FeVxf8KDfLEYhOFvjm+FjY=
ADMIN_EMAIL=admin@yoursite.com
ADMIN_PASSWORD=YourSecurePassword123!
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# 🌐 SINGLE DOMAIN SETUP (Same domain for frontend and backend)
# Replace "yoursite.com" with your actual domain
FRONTEND_BASE_URL=https://yoursite.com
API_BASE_URL=https://yoursite.com/api
JWT_ISSUER=https://yoursite.com
JWT_AUDIENCE=https://yoursite.com

# 🔗 CORS - Same domain, so very simple
CORS_ALLOWED_ORIGINS=https://yoursite.com,https://www.yoursite.com

# 📧 EMAIL SETTINGS (Required for email features)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your.email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
EMAIL_FROM=your.email@gmail.com
EMAIL_FROM_NAME=Your Portfolio
EMAIL_ENABLE_SSL=true

# 🗄️ DATABASE (SQLite by default - perfect for single server)
CONNECTION_STRING=Data Source=./wwwroot/App_Data/PortfolioDB.sqlite
DATABASE_PROVIDER=sqlite

# 📧 EMAIL PATHS (Default paths work great)
EMAIL_VERIFICATION_PATH=/auth/verify
PASSWORD_RESET_PATH=/auth/reset

# 🔒 PRODUCTION SECURITY
REQUIRE_HTTPS=true
USE_HSTS=true
REQUIRE_EMAIL_CONFIRMATION=false

# 🎥 EXTERNAL SERVICES (Optional)
YOUTUBE_API_KEY=
YOUTUBE_CHANNEL_ID=

# 📊 FOR DEVELOPMENT (localhost testing)
# Uncomment these for local development:
# FRONTEND_BASE_URL=http://localhost:3000
# API_BASE_URL=http://localhost:7151
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:7151
# REQUIRE_HTTPS=false
